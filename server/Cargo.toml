[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.42.0", features = ["full"] }
axum = { version = "0.7.9"}
tower-http = { version = "0.5", features = ["fs", "cors"] }
dotenv = "0.15.0"
