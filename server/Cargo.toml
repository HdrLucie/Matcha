[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.42", features = ["full"] }
axum = { version = "0.8" }
tower-http = { version = "0.6", features = ["fs", "cors"] }
dotenv = "0.15"
serde_json = "1.0"
