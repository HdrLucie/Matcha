services:
  frontend:
    build: ./client
    ports:
      - "5173:5173"
    volumes:
      - ./client/src:/app/src

  backend:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./server/src:/app/src
